require 'spec_helper'

describe "LegalEntities" do
  describe "GET /legal_entities" do
    it "works! (now write some real specs)" do
      get legal_entities_path
      expect(response.status).to be(200)
    end
  end
end
